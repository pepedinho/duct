[package]
name = "duct-network"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["full"] }
hyper = { version = "1.8.1", features = ["full"] }
hyper-tls = "0.6" # Pour le support HTTPS
http = "1.4.0"
